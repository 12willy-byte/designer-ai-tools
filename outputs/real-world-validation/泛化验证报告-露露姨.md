# 泛化验证报告：露露姨出租房平面（零调参）

日期：2026-07-21 ｜ 基线提交：add16fe ｜ 测试性质：**泛化能力测试，解析器参数零改动**

输入：`/Users/Apple/Documents/kimi/workspace/real-samples/露露姨出租房平面.pdf`
（1 页 3370×2384pt，lines=959 rects=38 curves=773 images=0 words=82；只有布置图，无结构图 → 单图路径，不触发 plan_fusion）

---

## 1. 零调参解析结果全景

运行：`read_pdf_plan(露露姨出租房平面.pdf)`，全量结果存 `parse_露露姨_零调参.json`。

### 比例校准 —— 优秀，即插即用

| 项 | 值 |
|---|---|
| pt_to_mm | 6.445794 |
| 校准来源 | dimension_text（标注数字反推，50 票） |
| scale_confidence | 0.85，dimensions_calibrated=True |

**用 11860mm 总开间独立验证**：

- 底部尺寸链 1520+650+5940+2430+1320 = **11860**，链上每段端点与墙体端点逐一吻合
  （如"5940"标注文字中心 x=10072，对应墙段 x 7102→13042 中点 10072，**长度分毫不差**）；
- 竖排反读总进深"0246"→6420mm，与左侧反读分段链之和 6390mm 相差 0.5% 以内；
- 竖排反读机制对本图同样生效（"0246/0441/0602/5159" 等反读标注参与投票且方向判定正确）。
- **6 位合并标注"146200"**：正读 146200 / 反读 002641 均被 3–5 位标注正则排除，未污染投票（安全，但信息损失，见 §4）。

### 房间闭环 —— 7 环，对 7 个真实房间仅 2 个可用

真实房间：客卫、次卧、主卧、走廊、厨房、阳台、客厅（出租房，约 60㎡）。

| 闭环 | 面积 | 判定 |
|---|---|---|
| 厨房 4.04㎡ | bbox 2.4×2.0m，名字在环内 | ✅ **可用** |
| 主卧 7.57㎡ | bbox 4.7×3.15m，L 形残环，名字在环内 | ⚠️ 部分可用（真实主卧应≈10㎡+） |
| 次卧 2.73㎡ | **宽仅 1.05m 的墙缝条带**，名字来自 0.45m 外的就近文字 | ❌ 名实不符 |
| 走廊 2.12㎡ | 顶部结构带碎片（y 10450–11850），"走廊"文字在 2.1m 外 | ❌ 名实不符 |
| 阳台 1.03㎡ | **宽仅 0.75m 的条带**，名字来自 1.2m 外 | ❌ 名实不符 |
| 未命名空间 22.82㎡ | 实为客厅+餐厅区（"客厅"文字距环 >2.5m，按诚实化规则未命名） | ⚠️ 几何大体可信、无名 |
| 未命名空间2 1.61㎡ | 0.55m 宽右缘条带 | ❌ 假房间 |
| 客厅 | **未闭环**（11 个 >4m 超大开口按设计不桥接） | ❌ 缺失 |
| 客卫 | 未闭环 | ❌ 缺失 |

**计分：7 个真实房间，1 个可靠 + 1 个部分 = 2/7 可用；3 个名字贴错碎片；2 个假房间。**

### 门窗计数 —— 门膨胀且语义弱，窗为零

- 门洞 26 个 = door 11 + opening 8 + unverified 7；
- 其中 **6 樘"door"宽度 2.26–3.81m**（单扇门物理不可能），是多个相邻洞口被弧线证据过度合并；
- 标准门宽（600–1100mm）的 door 仅 3 樘；该户型实际应有 ≈5 樘门（入户/主卧/次卧/客卫/厨房）；
- 门扇弧线佐证仅 6 条（本图弧线多来自家具，773 条 curves 中可用佐证少）；
- **窗户 = 0**：布置图无窗符号表达，检测器无从下手。

### 置信度与 limitations

- 整体置信度 **0.65**（罗菁单图 0.65 / 原始图 0.67 / 双图融合 0.69，同档）；
- limitations 如实记录了：100 条未配对单线、9 个虚拟桥接大洞口、11 个未桥接超大开口、
  2 个远距离命名、3 个房间含虚拟边界、门窗启发式需复核等——**该报的都报了**。

## 2. Bug 判定与修复

**判定：无崩溃、无常识性硬伤，按测试纪律未改任何解析器代码与阈值。**

逐项核查过的疑点：

1. "走廊"闭环 y 至 11850 是否越出外墙？→ 否，y=11864 处存在真实墙段（顶部结构带），闭环几何合法，是命名贴错而非越界；
2. 比例是否漂移？→ 否（§1，5940 墙段零误差）；
3. "146200"是否污染比例？→ 否，被位数正则排除；
4. 门过度合并是否回归？→ 与罗菁同套启发式，差异来自输入证据（本图无结构图互证、弧线多源于家具），属泛化差距而非新 bug。

**唯一改动是测试脚手架（非系统代码）**：首轮 conditions 把约束写进"出租房约束"键，
而 `_collect_constraints` 按**键名**（承重/水/电…）归类，导致闸门多出两条 layout_missing。
第二轮改用与 stage4 一致的"承重墙/上下水"键——这是条件文件的既有契约，不是给系统调参。

## 3. 端到端（真实模式 DeepSeek，M0→M6）

脚本：`outputs/_generalization_luluyi_run.py`；产物：`outputs/real-world-validation/generalization-luluyi/`
（含交付包 `露露姨出租房交付包/`）。manifest 标记 `generation.mode=real_ai, provider=deepseek`。

### 闸门判决（诚实，值得记录）

- allowed：concept_package、budget_estimate、material_palette
- **blocked：layout_draft** —— 唯一理由"缺少窗户/采光面位置"（窗户=0 的直接后果）；
  construction_docs 照例拦截。交付包 manifest 如实标注"不伪造其产物"。
- 对比：罗菁单结构图（windows=1）与双图融合均放行 layout_draft。

### 关键产物摘录

**设计定位（AI 生成，切题）**：
> 本案为60㎡两室一厅出租房，面向年轻租客（单人或情侣），以"简约实用"为核心理念……
> 整体设计强调耐用材料与低维护成本，以中性色调和经典元素适应不同租客审美……提升出租竞争力。

**预算（规则引擎算数，AI 只写建议）**：

- 总价区间 **8.5–13.9 万元** vs 预算 6 万 → 如实警示"明显超支"；
- AI 建议："优先压缩主材和全屋项目费用……室内门改用经济档免漆门"；
- ⚠️ **泛化差距的下游放大**：`室内门（含门套）23 樘 × 900–1400 = 2.07–3.22 万元`——
  工程量直接取门洞数 23，而实际约 5 樘（应 0.45–0.7 万）。门计数膨胀把最大超支项造了出来；
- 厨房分项合理（橱柜 3 延米、墙砖按周长−洞口），分房间小计随错误房间清单走
  （22.82㎡"未命名空间" vs 2.73㎡"次卧"，数额随之失真）。

## 4. 与罗菁深圳对比 & 泛化差距清单

| 指标 | 罗菁深圳（双图融合） | 露露姨（单图零调参） |
|---|---|---|
| 比例校准 | 可靠 | **同样可靠（零漂移）** |
| 真实房间覆盖率 | 高（9–10 环覆盖主要空间） | **2/7 可用** |
| 假房间 | 少量，已过滤 | 2 个条带假房间 + 3 个名贴错碎片 |
| 门 | 收敛 13（6 door+7 opening） | 26（11 door，含 6 樘 2.2m+ 超宽假门） |
| 窗 | 1 | **0 → 布局草案被闸门拦截** |
| 端到端 | 布局草案放行且覆盖主卧 | 布局拦截；概念/预算/材质放行 |

**差距清单（按优先级）**：

1. **家具线条污染闭环**：100 条未配对单线（家具/标注）参与成环，≥450mm/≥1㎡ 的退化过滤
   挡不住"1m 宽墙缝条带"——罗菁图纸家具少，此弱点未暴露；
2. **就近命名阈值过宽**：文字在环外 2.5m 内即贴名。本图房间名常写在环外（"次卧"贴在环外 0.45m 的
   真房间上，却被 1m 宽条带抢名）——需要"文字在环内优先、环外降权/标注存疑"的策略；
3. **门语义单图证据不足**：无结构图互证时，弧线（含家具弧线）会把相邻洞口合并成 3.8m"门"。
   需要门宽物理先验（>1.2m 的 door 降级为 opening）——**此规则对罗菁结果同样更合理**
   （其 door 均 ≤1m），属可论证的普适改进，但按纪律本次不动；
4. **窗户检测对布置图无能为力** → 闸门拦截布局草案。保守是对的，但意味着
   "只有布置图"这一最常见输入形态下，布局模块必然不可用；
5. **超宽洞口下游无隔离**：23 樘门直接进预算工程量，误差被放大成金额；
6. **6 位合并标注**（"146200"=两段标注粘连）双读均被排除，安全但丢信息；
7. 竖排反读文字（"桌书"←"书桌"）原样存储未回正——本次未造成后果，但命名类文字反读
   目前没有纠正通道。

## 5. 诚实结论：离"即插即用"还差什么

**能做到的（开箱即用部分）**：比例校准（双图验证零漂移）、竖排反读投票、双图融合框架、
诚实化机制（limitations/虚拟边界/闸门拦截/不伪造产物）在新图纸上全部复现，系统**能跑、不崩、不撒谎**。

**做不到的**：对"含家具线条的单张布置图"——最常见的真实输入——房间清单半数名实不符、
客厅/卫生间消失、门计数膨胀 4–5 倍、布局草案被窗户缺口拦截。用户拿到的概念/预算产物
建立在残缺空间事实上，必须人工复核后才能用。

**结论：这套系统对新图纸是"可安全试运行（fail-safe）"，远未到"即插即用"。**
打通最后一段需要的不是调阈值，而是四个结构性改进：家具线过滤、命名环内优先、
门宽物理先验降级、窗户缺失时的布局降级模式（如"仅家具摆放建议"而非整体拦截）。

## 6. 回归

11 个离线验收脚本（`AI_DEMO_MODE=1 python3 scripts/validate_*.py`）**全绿**，与基线一致。

## 7. 改进后复测（四项普适改进）

按差距清单落地的四项结构性改进（均为普适规则、零图纸特判、罗菁基线锁定后实施）：

1. **家具线条过滤**（`core/pdf_plan_reader.py`）：未配对单线做传递闭包分级——与墙线网连通的判
   structural（仍参与成环），孤岛判 floating（留痕 `furniture_lines`，闭环级过滤：
   净宽 <1m 物理下限剔除；面积 <3㎡ 且浮动单线覆盖 ≥50% 剔除；剔除留痕 `removed_rooms`）。
   实测真走廊覆盖率 0.30–0.36 与 1m 假条带 0.21 在 1.0–1.5m 区间无法干净区分，故该区间
   不做覆盖率剔除（已写入代码注释）。曾试"floating 排除出成环"导致罗菁房间塌缩，回退为
   "标记+闭环级过滤"。
2. **命名环内优先**：环内文字"占用"命名权；环外兜底只取未占用文字，且标
   `name_source=nearest_outside` + `name_confidence=low`。
3. **门宽物理先验**（`_DOOR_LEAF_MAX_MM=1200`）：弧线佐证但宽 >1.2m 的"门"降级为
   opening(0.4) 并留痕 `wide_arc_downgrade`；洞口侧翼规则同步收紧（两缘均非中线单线的
   gap 需门扇/窗佐证）。
4. **无窗降级模式**（`core/automation_gate.py` + `core/layout_draft.py`）：缺窗从
   `layout_missing`（硬拦截）移入新增 `layout_degraded`（降级），`layout_draft` 以
   `layout_mode="draft_degraded"` 生成"仅家具摆放讨论稿"，摘要含醒目 banner 声明
   "采光面未知……需人工补充"；缺门洞/结构/机电仍硬拦截。
   **联动修复**：`core/budget_estimate.py` 新增 `_counts_as_interior_door`（door→计；
   opening ≤1.2m→计；超宽/unverified→不计），堵住"门计数膨胀→预算放大"通道。

### 双图前后对比

| 指标 | 露露姨（改前→改后） | 罗菁（改前→改后） |
|---|---|---|
| 比例校准 | 6.4458 → 6.4458（零漂移） | 7.20777 → 7.20777（零漂移） |
| 房间 | 7 环仅 2 可用 → **5 环**（未命名空间 22.82 / 主卧 7.57 / 厨房 4.04 / 次卧 2.73* / 客卫 2.12*，*低置信兜底），剔除 3 假环 | 9 房 → 9 房 |
| 融合房间 | —（单图） | 10 房（含主卧 plan_only 5.50㎡）→ 10 房 |
| 门 | 26（door 11，含 6 樘超宽假门）→ **26（door 3 + opening 16 + unverified 7），超宽假门清零**，8 处 wide_arc_downgrade | 13（6 door+7 opening）→ 13 |
| 窗 | 0 → 0 | 10 → 10 |
| 布局草案 | 闸门拦截 → **draft_degraded 降级生成** | 放行 → 放行 |
| 预算门分项 | 23 樘 2.07–3.22 万 → **5 樘 0.45–0.70 万** | 附着 9 樘 → 8 樘（1.12–1.76 万，变准非变差） |
| 预算总价 | 8.5–13.9 万 → **5.81–9.54 万** | 口径一致 |

罗菁唯一差异：6.08㎡ 环由"客厅"改标"未命名空间3"（"客厅"文字已被 63.14㎡ 复合环环内占用，
系环内优先规则的正确行为，属等价波动而非退化）；merged 置信 0.82 不变。

### 布局降级产物摘录（真实模式 e2e，deepseek）

> ⚠️ 采光面未知：本图纸未检测到窗户，以下仅为家具摆放讨论稿，窗位/采光需人工补充后方可施工。
> 项目……房间 5 个，门 21 个，窗 0 个。

各房间家具清单与门窗约束正常生成；产物在
`outputs/real-world-validation/generalization-luluyi-after/`。

### 回归

新增 `scripts/validate_furnished_generalization.py` 把露露姨改进断言 + 罗菁基线锁定
（7.20777 / 9 房 / 融合 10 房 / 13 门 / 10 窗）+ 降级闸门 + 预算口径固化进验收；
**12 个离线验收脚本全绿**。

### 更新判断

即插即用程度由"可安全试运行（fail-safe）"升为 **"含家具单布置图可降级可用"**：
概念/预算/材质产物可直接用（门分项已回归物理量级），布局为降级讨论稿
（需人工补窗位），房间命名需复核低置信项。**仍非即插即用**，剩余差距：

- 客厅 22.82㎡ 复合环名贴不上（"客厅"文字距环 >2.5m，超兜底阈值）；
- 次卧 2.73㎡ 条带环名实不符，仅低置信标注提示；
- door_details 总数仍 26（语义已收敛但假洞未物理清除）；
- 走廊/阳台仍缺；主卧 7.57㎡ 为 L 形残环；散线墙成环质量仍依赖连通性启发式；
- 流程图渲染产物（`resources/floorplan-ai-workflow/更新.png`）待按"降级模式"新流程重绘。
