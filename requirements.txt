openpyxl>=3.1
Pillow>=10.0
python-pptx>=0.6.23
ezdxf>=1.3
# 矢量 PDF 图纸解析（M0 第三输入路径）。
# 选 pdfplumber 而非 PyMuPDF：pdfplumber 为 MIT 协议（PyMuPDF 是 AGPL，
# 项目若未来商用有传染性风险），且纯 Python 生态、矢量线段/矩形/文字坐标
# 提取能力满足图纸解析需求。
pdfplumber>=0.11
# 仅用于生成测试用矢量 PDF 样本（scripts/generate_sample_pdf_plan.py）。
reportlab>=4.0
# Web 产品化界面后端（webapp/server/app.py）。
Flask>=3.0
Flask-Cors>=4.0
